## `$unset, $pop, $pull $pullAll`

**`$unset`**

```javascript
// update a single field {$unset delete field}
db.test.updateOne(
  { _id: ObjectId("6406ad63fc13ae5a40000065") },
  { $unset: { birthday: "" } }
);
```
